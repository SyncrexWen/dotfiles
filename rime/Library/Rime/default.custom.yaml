patch:

schema_list:
    - schema: luna_pinyin
    - schema: med_pinyin

  ascii_composer/good_old_caps_lock: true
  ascii_composer/switch_key:
    Shift_L: commit_code  # L Shift: Directly input and switch to en
    Shift_R: commit_code  # R Shift: Same to above
    Control_L: noop
    Control_R: noop
    Caps_Lock: clear
    Eisu_toggle: clear
